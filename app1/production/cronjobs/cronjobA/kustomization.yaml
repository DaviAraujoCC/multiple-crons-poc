apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base

configMapGenerator:
- name: cronjobA-cron-env
  namespace: default
  envs: [cronjobA.env]
  behavior: merge
  options:
    disableNameSuffixHash: true

patchesStrategicMerge:
  - cronjobA.yaml

patches:
- target:
    kind: CronJob
  patch: |-
    - op: replace
      path: /metadata/name
      value: cronjobA
    - op: replace
      path: /spec/jobTemplate/spec/template/spec/containers/0/name
      value: cronjobA